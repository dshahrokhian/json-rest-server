package sneakPeek

object Masks {
  val RED = 16; /* Shifting with this mask will only change the
                   Red byte in a ARGB Int */
  val ALPHA = 24; /* Shifting with this mask will only change the
  									 Alpha byte in a ARGB Int */
}